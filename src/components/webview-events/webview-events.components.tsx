import React from 'react';
import type { FC } from 'react';

const WebviewEvents: FC = () => {
  return (
    <>
      <div id={'chartEvents'}></div>
      <div id={'chartEventsListen'}></div>
    </>
  );
};

export default WebviewEvents;
